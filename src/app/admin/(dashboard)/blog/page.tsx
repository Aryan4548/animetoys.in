import ComingSoon from "@/components/admin/ComingSoon";

export default function AdminBlogPage() {
  return (
    <ComingSoon
      title="Blog"
      description="Write and publish blog posts with a cover image, tags and scheduled publish date. The public /blog pages already render from the BlogPost collection — this screen adds the editor UI."
    />
  );
}
