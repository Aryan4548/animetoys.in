import ComingSoon from "@/components/admin/ComingSoon";

export default function AdminCouponsPage() {
  return (
    <ComingSoon
      title="Coupons"
      description="Create percentage or fixed-value discount codes with usage limits, minimum order values and expiry dates."
    />
  );
}
