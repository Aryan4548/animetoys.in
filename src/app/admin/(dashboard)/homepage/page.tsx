import ComingSoon from "@/components/admin/ComingSoon";

export default function AdminHomepageCmsPage() {
  return (
    <ComingSoon
      title="Homepage CMS"
      description="Reorder, hide/show and edit the copy for each homepage section (hero, category strip, preorder/wholesale banners, new arrivals, featured, community, newsletter) without a deploy."
    />
  );
}
